const state = {
  activities: []
}

const getters = {
  getActivities: state => state.activities
}

const actions = {
  async fetchActivities() {
    
  }
}

const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}