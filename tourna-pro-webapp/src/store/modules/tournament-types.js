const state = {
  tournamentTypes: []
}

const getters = {
  getTournamentTypes: state => state.tournamentTypes
}

const actions = {
  async fetchTournamentTypes() {
    
  }
}

const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}