const state = {
  achievements: []
}

const getters = {
  getAchievements: state => state.achievements
}

const actions = {
  async fetchAchievements() {

  }
}

const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}